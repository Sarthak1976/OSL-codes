#include<stdio.h>
#include<unistd.h>
#include<stdlib.h>
#include<sys/types.h>

int main(){
	int a[5];
	int i;
	for(i = 0;i < 5;i++){
		scanf("%d", &a[i]);

	}

	pid_t ret;
	ret = fork();

	if(ret > 0){
		printf("\nThe parent process with pid %d\n",getpid());
		for(int i = 0;i < 5;i++){
			for(int j = 0;j < 4-i;j++){
				if(a[j] > a[j+1]){
					int temp = a[j];
					a[j] = a[j+1];
					a[j+1] = temp;
			
				}

			}
		}

		char *args[5+2];
		args[0] = "./child";
		for(i = 0; i < n; i++) {
            		char *num = malloc(10); // allocate memory to store int as string
            		sprintf(num, "%d", arr[i]);
            		args[i + 1] = num;
        	}
        	args[n + 1] = NULL;

        	execve("./child", args, NULL);
        	perror("execve failed");

	}else if(ret == 0){
		
		
		
	}else{
		perror("fork failed");
	
	}

	return 0;
}
